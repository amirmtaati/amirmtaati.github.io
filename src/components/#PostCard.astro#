---
// src/components/PostCard.astro
---
export interface Props {
  post: {
    title: string;
    slug: string;
    excerpt: string;
    date: Date;
    tags: string[];
  }
}

const { post } = Astro.props;
import Tag from './Tag.astro';
---

<article class="mb-12">
  <a href={`/blog/${post.slug}`}>
    <h2 class="heading text-2xl mb-3 hover:text-gl-accent dark:hover:text-gd-accent transition-colors">
      {post.title}
    </h2>
  </a>
  
  <div class="flex items-center gap-4 mb-4 text-sm">
    <time class="text-gl-fg2 dark:text-gd-fg2">

    </time>
    
    <div class="flex gap-2">
      {post.tags.map((tag) => (
        <Tag name={tag} />
      ))}
    </div>
  </div>
  
  <p class="text-gl-fg2 dark:text-gd-fg2 mb-4">
    {post.excerpt}
  </p>
  
  <a 
    href={`/blog/${post.slug}`} 
    class="text-sm font-medium text-gl-accent dark:text-gd-accent hover:underline"
  >
    Read more →
  </a>
</article>
